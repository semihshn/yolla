version: '3.9'

services:

  redis:
    image: "redis:alpine"
    command: redis-server --requirepass redisPassword
    ports:
      - "6379:6379"
    networks:
      - default

#docker network create yolla_network
networks:
  default:
    name: yolla_network
    external: true